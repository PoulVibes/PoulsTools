-- DynamicBuffTracker_Defaults.lua
-- Default override icon IDs and condition timers per spell ID.
-- Priority at display time: saved value > default > empty.

DynamicBuffTracker_Defaults = {
    [1253599] = { icon = 3636840, timer = 12 },
}
